# 我在你不在場時做的所有裁決

格式：決定什麼 — 為什麼 — 如果我判斷錯了，代價是什麼

- Ruling: Task 1 測試數 8→9、Task 5 累計測試檔 4→5 — 計畫的斷言數字與實際 `it` 數量不符，實作者會誤以為多跑了測試而回頭找問題 — 若改錯只是期望值標示偏差，實際仍以「全綠」為準。
- Ruling: Task 9 的 app-store import 指示改為明述「取代既有那行」 — 原文「補上 import」會被讀成新增，造成 `Board` 重複 import 而編譯失敗 — 若多此一舉，成本僅為一句贅述。
- Ruling: 移除 Task 11 CommandPalette 的 `listRef` — 宣告後未使用，reviewer 會判為 dead code 並進入修復迴圈 — 若日後需要捲動到選取項，再加回即可。
- Ruling: 以 branch `feat/sharkcommand-mvp` 取代 git worktree 作為隔離工作區 — repo 僅含兩份 docs commit、無並行工作，worktree 的額外目錄對單人新專案是純負擔 — 若需並行開發，隨時可從此 branch 再開 worktree。
- Ruling: package.json 初始不含 postinstall，改在所有依賴安裝完成後以 `npm pkg set` 補上並手動執行 — 第一次 `npm install` 時 electron-builder 尚未存在，postinstall 會失敗並中止整個安裝 — 若判斷有誤，成本僅為多一道手動指令。
- Ruling: React 釘在 18（含 @types/react@18） — React 19 型別移除全域 JSX namespace，而計畫每個元件都以 `JSX.Element` 標註回傳型別，裝 19 會全面型別錯誤 — 若日後要升 19，需一併把回傳型別改為 `React.JSX.Element` 或移除標註。
- Ruling: 捨棄 project references，改用兩份獨立 tsconfig（tsconfig.json = renderer、tsconfig.node.json = main/preload/tests） — `composite: true` 與 `noEmit: true` 是 TypeScript 禁止的組合，原設計無法通過型別檢查 — 代價是編輯器對 main 程序檔案改用 fallback 設定，對本專案無實質影響。
- Ruling: tailwindcss 降至 ^3（實裝 3.4.19） — npm 預設抓到 v4，其 CSS-first 設定語法與計畫給的 v3 `tailwind.config.js` 及 `@tailwind` 指令不相容，build 直接失敗 — **後續所有 UI 任務（7、8、9、10、11）必須沿用 v3 語法**，dispatch 時要明講；若要升 v4 得重寫設定檔與 index.css。
- Ruling: typescript 降至 ^5（實裝 5.9.3） — npm 抓到 7.0.2（Go 移植版）已移除 `baseUrl`，與計畫兩份 tsconfig 的 `baseUrl` + `paths` 衝突（TS5102） — 若要升 TS7，需改用不依賴 baseUrl 的 path 對應方式。
- Ruling: vite 釘 ^7 搭配 @vitejs/plugin-react ^5 — 兩者最新版之間 ERESOLVE 衝突 — 若日後 plugin 更新解除衝突，可放寬。
- Ruling: 欄位色盤維持定義在 `src/shared/types.ts` 的 `COLUMN_COLORS`，不搬進 tailwind.config.js — 欄位顏色是執行期動態指派、以 inline style 渲染，Tailwind 的 JIT 只掃描原始碼中的靜態字串，無法為動態色碼產生 utility class；是我 dispatch 時把「視覺 token 表」整份當成 tailwind.config 內容轉述，造成 reviewer 認知落差，brief 本體的設計是對的 — 後續 dispatch 的 constraint 措辭已修正為區分「Tailwind token」與「JS 常數」；若誤判，代價是欄位色無法用 Tailwind class 套用（但本來就不該用）。
- Ruling: `package.json` 缺 `engines.node` 列為 deferred minor，不在此任務修 — Node 20.20.2 下 `@electron/rebuild`/`node-abi` 僅產生 EBADENGINE warning，未阻斷，且 Task 1 已實證 `install-app-deps` 與 dev build 皆成功 — **但 Task 11 打包（`--universal`）時必須先驗證 electron-builder 在 Node 20 下可完成，dispatch 要明講**；若屆時失敗，需請使用者升級 Node 至 22.12+。
- Ruling: 測試輸出出現 `[MODULE_TYPELESS_PACKAGE_JSON]` warning（postcss.config.js 用 ESM
- Ruling: 接受 Important finding（4 個 guard 分支零測試覆蓋 + moveColumn 缺 clamp 測試），進入修復迴圈而非以「計畫沒要求」駁回 — 計畫的作者身份不能為自己的產出打分數；這 4 個 guard 正是全域約束明訂必須以 console.warn 記錄的異常輸入防護，沒有測試就無法保證它們真的提前 return，未來重構讓 guard 失效時現有測試不會有任何反應 — 補 6 個小測試的成本遠低於防護網靜默失效的代價；若判斷有誤，代價僅是多 6 個測試。
- Ruling: 接受複審對項 6 的判定並列為必修 — `moveColumn(fixture(), 'col_0', -5)` 選了本來就在索引 0 的欄位，clamp 後期望值與 fixture 原始順序完全相同，即使 moveColumn 損毀成 no-op 也會通過，屬斷言恆真 — 這比原本沒有測試更糟，因為它製造了「已覆蓋」的假象；改用 col_2 即可觀察到實際位移。
- Ruling: 將 console.warn 斷言**一致套用到全部 guard 測試**（含既有的 updateCard、moveCard×2，原屬複審的範圍外觀察） — 只補新測試會讓同一檔案內出現兩種標準，下一輪 review 必然再提；全域約束明訂 fallback 分支必須記錄上下文，沒有斷言就無法阻止有人拿掉 log 而測試照過 — 成本是同一次編輯多改三行；若判斷有誤，代價僅是三個多餘斷言。
- Ruling: ⚠️「curl 驗證 Tailwind token 已編譯進 CSS」不另行覆核 — 核心驗收（47/47 測試綠、
- Ruling: 立即修正計畫文件中 4 處舊 config 檔名（`.js` → `.mjs`），不列為 deferred —
- Ruling: 接受 Important #1（load() 裸 catch 誤判所有讀檔錯誤為首次啟動）並列為必修 — EACCES/EISDIR/EMFILE 等錯誤下 board.json 確實存在卻讀不到，現行程式會用預設看板 rename 覆蓋它，使用者整個看板無聲消失且無備份無 log，比損毀路徑更糟（損毀至少會備份）— 修法：只有 ENOENT 才視為首次啟動；其他錯誤記錄 console.error、回傳預設看板供 app 啟動，並設 readOnly 旗標讓 save/flush 拒絕寫入，避免後續操作覆蓋原檔 — 若判斷有誤，代價是罕見情境下該工作階段不儲存（可從 log 診斷）。
- Ruling: 接受 Important #2（writeAtomic 清理 .tmp 的 fs.rm 未受保護造成未處理 Promise rejection）並列為必修 — force:true 只吞 ENOENT，EACCES/EPERM 仍會拋出，一路傳到 save() 的 `void this.flush()`；Node 15+ 未處理 rejection 預設終止程序，在 Electron main 會讓整個 app 掛掉 — 修法：清理包自己的 try/catch，另在 debounce 回呼補 .catch() 雙重保險。
- Ruling: 同步修正計畫文件中的對應程式碼，不只修實作 — 這兩處缺陷是計畫本身寫的，若只修產品程式碼，計畫仍留著錯誤範本，未來重跑或他人閱讀會重蹈覆轍。
- Ruling: 接受 Critical（spawn 重啟時舊 pty 延遲觸發的 onExit 誤刪新 pty 並廣播假 exit）— 真實 node-pty 的行程死亡回報必然非同步，時序保證是「新 pty 先進 Map → 舊 pty 的 onExit 才觸發」，因此每次重啟卡片都必然踩到，不是機率性 race；後果是新啟動的 process 變成無法追蹤的孤兒、UI 誤報已結束，且若在該視窗內 killAll 會漏送 SIGKILL 而在 app 關閉後遺留行程 — 修法選「onExit 內比對 this.ptys.get(cardId) === pty 才動作」而非保存 IDisposable，因為前者更簡單且不依賴 fake pty 模擬 dispose 語意 — 若判斷有誤，代價是多一次身分比對。
- Ruling: 接受 Important #2（write 未受 try/catch 保護）— 同檔的 resize/kill 都有保護，write 沒有；對一個行程已死但 exit 尚未回報的 pty 打字時若底層拋出，會 propagate 到 IPC handler 造成 Electron main 例外。
- Ruling: 接受 Important #3（kill 對不存在 cardId 靜默 return）— 同一情境在 write/resize 有 warn、kill 沒有，三者不一致會讓重複 kill 或錯誤 cardId 的 bug 被靜默吞掉。
- Ruling: 單卡 kill() 直接送 SIGKILL 不改 — Global Constraint 的「SIGTERM → 500ms → SIGKILL」是針對 app 關閉的 killAll 流程；使用者按「停止」是明確意圖，且 brief 測試明確要求 kill 送 SIGKILL — 記為 deferred 供 final review 考慮是否要給 Claude 清理機會。
- Ruling: Task 4 那條 deferred minor（writeAtomic 的 .tmp 檔名非唯一）升級為必修 — 它在 Task 6 已實證發生：
- Ruling: 接受 Important #1（webContents.send 未檢查 isDestroyed）— mainWindow 只在 'closed' 才設 null，而 webContents 在關窗流程中先被銷毀，中間時間差內 `?.` 擋不住已銷毀物件，.send() 會同步拋例外；macOS 關窗不等於結束 app、背景 pty 持續產出 data，此路徑必然被走到；例外在 data callback 拋出會讓 main process 崩潰，崩潰前不走 before-quit，等同資料遺失路徑 — 修法：取出 win 後檢查 `win && !win.isDestroyed()`。
- Ruling: 接受 Important #2（四個 ipcMain.handle 無 try/catch 與結構化 log）— 例外會轉成 renderer 端 rejected promise，main 端不留任何診斷紀錄，違反全域錯誤記錄約束；Task 7 接上真實使用者輸入後，pty:spawn 因無效目錄或指令不存在而失敗的機率遠高於現在，屆時完全沒有線索可查。
- Ruling: 一併採納 Minor #3（before-quit 的 app.quit() 改 app.exit()）— 連按兩次 ⌘Q 時第二次 before-quit 因 cleaningUp 已為 true 而不 preventDefault，可能搶在 flush 完成前退出；成本一行，消除的是資料遺失視窗。
- Ruling: 額外要求加入 dev 模式的 renderer console 轉發（webContents 的 console-message 事件轉印到 main 的 stdout）— 這不在 brief 內，但至今所有 subagent 都看不到 renderer 的 console，等於 renderer 端的錯誤完全沒有觀測能力；Task 7 之後 UI 任務全在 renderer，缺這個能力等於盲修 — 若判斷有誤，代價是 dev 模式多幾行 log 輸出。
- Ruling: 額外要求做一次 IPC 端到端自動探針 — reviewer 正確指出 node-pty 探針繞過了 preload/contextBridge/ipcMain 整條鏈路，等於本任務的核心交付「接線」本身尚未經過任何一次端到端執行；board:save、pty:write/resize/kill、dialog:pickDirectory 五個 channel 完全沒被觸發過 — 讓驗證版 App.tsx 自動跑一輪完整鏈路並經 console 轉發輸出，比留到 Task 7/9 才發現接線錯誤的除錯成本低得多。
- Ruling: BoardStore 的 tmp 檔名唯一化併入本輪由 impl-task6 執行（而非另派 impl-task4）— 兩者若並行修改同一工作區會使 commit 邊界混亂；責任歸屬記於此，並在 re-review 時明確要求複審該項。
- Ruling: 不採納 Minor #1（ipc 層對 board:load 加 in-flight 去重）— StrictMode 雙重 mount 只存在於 dev，production build 不會發生；tmp 檔名唯一化後併發寫入已無害，再加一層去重是為 dev-only 現象增加正式程式碼複雜度 — 列為 deferred，若 Task 7 正式 UI 出現實際問題再處理。
- Ruling: 修 kill() 的語意 — 我在 Task 5 修好「重啟時誤刪新 pty」的同時打破了「主動 kill 的 exit 通知」，
- Ruling: 接受 implementer 對 app.exit() 殘留競態的疑慮並修 — 它誠實指出我的指示只解決了自我觸發那半，
- Ruling: `killAll()` 的 SIGKILL 迴圈有與 kill() 相同模式的缺陷（先 delete 再送信號，使該 pty 真正
- Ruling: **駁回** Important #1（CardDialog.canSubmit 未驗證 command 欄位）— reviewer 從型別推論「空指令是缺陷」，
- Ruling: 接受 Important #2（pty.kill 未包 try/catch）— preload 的 pty.kill 是 ipcRenderer.send 的 fire-and-forget，
- Ruling: 接受 Important #1（過場動畫 200ms 超出 ≤150ms 全域約束）— dnd-kit 的 defaultTransition
- Ruling: Important #2（Step 7 人工拖拉驗證未執行）**不進 fix loop，轉為人工驗證項** — 這不是實作缺陷，
- Ruling: 採納 Minor #3（onDragEnd 的 !over 改為取消語意）— reviewer 判斷 !over 與 Esc 是同一類使用者意圖
- Ruling: 不採納 Minor #1（同欄同索引的重複 preview）列為 deferred — reviewer 假設 onDragOver 每幀觸發，
- Ruling: 接受實作者自行發現的 deleteColumn ptyStatus 洩漏並修 — TerminalHost 用 Object.keys(ptyStatus)
- Ruling: 疑慮 2（loadBoard 在 StrictMode 雙重呼叫）維持 deferred — 與 Task 6 的裁決一致，StrictMode 僅存在
- Ruling: 接受 Important #1（刪卡片後延遲送達的 onPtyExit 會把 ptyStatus 憑空復活）— 這是**我在 Task 6
- Ruling: 接受 Important #2（Splitter 未 setPointerCapture）— 拖曳時指標移出視窗放開會讓 pointerup 收不到，
- Ruling: 接受 Important #3（onChange 未 useCallback 導致拖曳中反覆重綁 window 監聽器）— 用 useCallback 穩定
- Ruling: 將使用者的「那就繼續做」視為對整個計畫執行的持續授權，包含本地 commit — 該句發生在我提出 commit 授權問題之前，使用者尚未看到該問題；CLAUDE.md 要求「Leader 在人類確認後執行」，而使用者已明確授權繼續執行計畫，commit 是計畫每個任務的收尾步驟；且 commit 全在本地功能分支、可隨時 reset — push 與開 PR 仍會逐次詢問；已在回覆中明確告知此裁決，使用者可隨時推翻。
- Ruling: 採納實作者對 StatusDot 的偏離（inline hex → Tailwind token class）— brief 用 inline hex 與全域約束
- Ruling: **推翻我自己在 Task 6 與 Task 9 的兩次裁決**，loadBoard 併發改判為必修 — 前兩次我判「dev-only、無害」，
- Ruling: 接受 Important（loadBranches 的 Promise.all 使單一 cwd 失敗即整批不更新）並修，儘管複審自己判定
- Ruling: 接受雙架構 .dmg 取代 spec 要求的 universal binary — node-pty 的 prebuild 在 lipo 合併階段衝突是
- Ruling: 接受實作者連動移除 package.json 的 --universal 旗標 — brief 未明寫但 fallback 要生效必須連動，
- Ruling: 接受 Important #1（cursor 下界 clamp 缺失）— rows 為空時 ArrowDown 把 cursor 設為 -1，而 clamp
- Ruling: 接受 Important #2（⌘K 與 xterm 焦點衝突未被真正驗證）— **這是我設計的驗證方法有漏洞，不是程式碼問題**：
- Ruling: 順帶修兩個 Minor — CommandPalette 靜默跳過查無對應的 cardId（補 console.warn，符合全域約束精神）、
- Ruling: C 點列為 deferred 不現在修 — 修法（新行程等舊行程 flush 完成，或存檔前檢查 mtime）都需要動
- Ruling: A 點（crash 假設驗證不足）列為 deferred — 複審讀過實際出貨的 Column.tsx 與 BoardPane.tsx，
- Ruling: 四條全部立即修並重新打包 — 兩個 Critical 都會造成不可逆的資料/工作遺失，且 C1 是
- Ruling: 未修 I4（單卡 kill 直送 SIGKILL 可能留下孤兒 claude 進程）— 審查者列為「必須驗證
